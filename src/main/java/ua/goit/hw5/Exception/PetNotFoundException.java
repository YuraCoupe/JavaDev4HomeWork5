package ua.goit.hw5.Exception;

// Looks like unused class. Do we need it? We don't need creating anything for the future, if we don't need it now - we have to delete it
public class PetNotFoundException extends RuntimeException {
    public PetNotFoundException(String message) {
        super(message);
    }
}
